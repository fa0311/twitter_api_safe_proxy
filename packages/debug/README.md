# twitter-api-safe-debug

Debug UI for Twitter/X web API requests captured through `twitter-api-safe-request`.

```sh
twitter-api-safe-debug --user-data-dir ./user_data/account1
```

Run the React UI separately:

```sh
pnpm --filter twitter-api-safe-debug dev:ui
```

Open `http://localhost:3002`.
